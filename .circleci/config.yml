version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  lint:
    docker:
      - image: cimg/node:16.18.0
    steps:
      - checkout
      - run: yarn
      - run: yarn run lint
  test:
    docker:
      - image: cimg/node:16.18.0
    steps:
      - checkout
      - run: yarn
      - run: yarn run build
      - run: yarn run test

workflows:
  version: 2.1
  lint-and-test:
    jobs:
      - lint
      - test
